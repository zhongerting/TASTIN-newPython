# 液态金属冷却剂集流环局部阻力系数调研整理

## 1. 建模目标

本文用于整理液态 NaK 集流环一维水力模型中，进出口三通、分流、汇流、突扩、突缩和混合节点带来的局部压降计算方法。

当前模型中集流环主体、热管管束压降已经建模，但以下连接位置的局部损失多数仍取 `k_loss=0`，需要补充合理的工程初值：

1. `inlet header -> hot leg`
2. `hot leg -> inlet mix`
3. `inlet mix -> ring segment`
4. `ring segment -> outlet mix`
5. `outlet mix -> manifold`
6. `manifold -> outlet header`

核心原则是：**每个局部阻力系数必须明确参考速度基准**。若资料中的 K 与程序 `FlowJunction.k_loss` 使用的速度基准不同，必须做面积/流量换算。

## 2. 当前几何与流量

### 2.1 几何参数

| 部位 | 参数 |
|---|---:|
| 集流环矩形通道长度 | 110 mm |
| 集流环矩形通道宽度 | 40 mm |
| 集流环流通面积 `A_ring` | 0.0044 m2 |
| 集流环水力直径 `D_h_ring` | 0.05867 m |
| 进口支管 hot leg 半径 | 13.8 mm |
| 进口支管 hot leg 面积 `A_hot_leg` | 5.98e-4 m2 |
| 出口支管 manifold 半径 | 9 mm |
| 出口支管 manifold 面积 `A_manifold` | 2.54e-4 m2 |
| 进口总管面积 | 约 `3 * A_hot_leg = 1.79e-3 m2` |

### 2.2 流量口径

实际系统有两个对称并联集流环。当前模型显式建模一个集流环，另一个通过 `MacroFlowJunction(multiplier=2)` 等效处理。

```text
系统总流量         W_total = 1.3 kg/s
单个集流环流量     W_ring  = 0.65 kg/s
单个进口/出口支管  W_leg   = 0.65 / 3 = 0.2167 kg/s
单个环段分支流量   W_seg   = 0.2167 / 2 = 0.1083 kg/s
```

若暂取 NaK 密度 `rho = 850 kg/m3`，速度和动压量级为：

| 位置 | 质量流量 | 面积 | 速度量级 | 动压 `0.5*rho*v^2` |
|---|---:|---:|---:|---:|
| hot leg | 0.2167 kg/s | 5.98e-4 m2 | 0.43 m/s | 约 77 Pa |
| ring segment | 0.1083 kg/s | 4.4e-3 m2 | 0.029 m/s | 约 0.36 Pa |
| outlet manifold | 0.2167 kg/s | 2.54e-4 m2 | 1.00 m/s | 约 428 Pa |

因此，出口侧 `outlet mix -> manifold` 和 `manifold -> outlet header` 最可能贡献主要局部压降。

## 3. 局部压降通用形式

局部压降统一写为：

```text
Delta_p = K_ref * 0.5 * rho * v_ref^2
```

其中：

- `K_ref`：资料或手册给出的局部阻力系数；
- `v_ref`：该 K 对应的参考速度；
- `rho`：流体密度；
- `Delta_p`：局部压降。

程序 `FlowJunction.k_loss` 的压降形式可理解为：

```text
Delta_p = K_program * 0.5 * rho * v_junction^2
v_junction = W / (rho * A_junction)
```

所以，如果资料中的 K 不是基于程序连接面积速度，需要换算：

```text
K_program = K_ref * (v_ref / v_junction)^2
```

用质量流量和面积表示：

```text
K_program = K_ref * ((m_ref / A_ref) / (m_junction / A_junction))^2
```

如果 `m_ref = m_junction`，则简化为：

```text
K_program = K_ref * (A_junction / A_ref)^2
```

## 4. 可采用的局部阻力模型

### 4.1 突扩损失

适用位置：

- `hot leg -> inlet mix`
- 小支管进入较大混合腔或集流环截面

推荐使用 Borda-Carnot / Idelchik 型突扩公式：

```text
K_exp = (1 - A_small / A_large)^2
```

参考速度：

```text
v_ref = v_small = Q / A_small
```

对当前 `hot leg -> inlet mix`：

```text
A_small = A_hot_leg = 5.98e-4 m2
A_large = A_ring = 4.4e-3 m2
A_small / A_large = 0.136
K_exp = (1 - 0.136)^2 = 0.75
```

工程建议：

```text
hot leg -> inlet mix: K = 0.7 - 1.5
推荐初值: K = 1.0
速度基准: hot leg 速度
```

若存在锐边入口、短混合腔、强转弯或明显回流，可取上限。

### 4.2 突缩损失

适用位置：

- `outlet mix -> manifold`
- 大混合腔或集流环截面进入小出口支管

推荐使用 Idelchik / Crane 型突缩公式：

```text
K_con = 0.5 * (1 - A_small / A_large)^0.75
```

工程简化也可用：

```text
K_con = 0.5 * (1 - A_small / A_large)
```

参考速度：

```text
v_ref = v_small = Q / A_small
```

对当前 `outlet mix -> manifold`：

```text
A_small = A_manifold = 2.54e-4 m2
A_large = A_ring = 4.4e-3 m2
A_small / A_large = 0.0577
K_con = 0.5 * (1 - 0.0577)^0.75 = 0.48
```

但该位置通常还包含汇流后加速、转弯和混合腔非均匀入口，因此建议工程初值放大：

```text
outlet mix -> manifold: K = 0.8 - 1.5
推荐初值: K = 1.0
速度基准: manifold 速度
```

### 4.3 三通分流：`inlet header -> hot leg`

适用对象：

- 进口总管向 3 个 hot leg 分配流量；
- T-junction dividing flow；
- 支管分流路径。

推荐优先查 Idelchik 或 Miller 的 tee dividing flow 图表/公式。常见变量包括：

```text
r = Q_branch / Q_main_in
alpha = A_branch / A_main
theta = 支管夹角
```

当前若 3 个 hot leg 均分：

```text
Q_branch / Q_main_in = 1/3
A_branch / A_main ≈ 1/3
```

支管速度和入口总管平均速度接近。可先用工程初值：

```text
inlet header -> hot leg: K = 1.0 - 2.0
推荐初值: K = 1.5
速度基准: hot leg 速度或等效入口 header 速度
```

如果 3 个支管沿总管顺序布置，而不是理想对称三分流，则每个支管的局部流量比不同，后续应分别给 K。

### 4.4 三通汇流：`manifold -> outlet header`

适用对象：

- 3 个 outlet manifold 汇入出口总管；
- T-junction combining flow；
- 支管汇流路径。

推荐优先查 Idelchik 或 Miller 的 tee combining flow 图表/公式。常见变量包括：

```text
r = Q_branch / Q_main_out
alpha = A_branch / A_main
theta = 支管夹角
```

当前若 3 个 outlet manifold 均匀汇流：

```text
Q_branch / Q_main_out = 1/3
A_branch / A_main ≈ 1/3
```

可先取：

```text
manifold -> outlet header: K = 0.8 - 1.5
推荐初值: K = 1.1
速度基准: manifold 速度或合流后 outlet header 速度
```

由于 manifold 面积小、速度高，这一项很可能是重要压降来源。

### 4.5 进口混合节点：`hot leg -> inlet mix -> two ring segments`

该位置不是单一标准 tee，而是复合构型：

1. hot leg 进入较大混合腔，有突扩；
2. 混合腔向左右两个 ring segment 分流；
3. 流动方向可能发生 90 度偏转；
4. 混合腔内可能有回流和非均匀速度分布。

建议拆分到两个程序连接：

| 连接 | 建议承载的物理损失 | 速度基准 | 推荐 K |
|---|---|---|---:|
| `hot leg -> inlet mix` | 突扩、进入混合腔、分流前损失 | hot leg 速度 | 0.8 - 1.5，初值 1.0 |
| `inlet mix -> ring segment` | 混合腔向左右环段分配的附加损失 | ring segment 速度 | 0.2 - 1.0，初值 0.5 |

不要把基于 hot leg 速度的损失直接重复加到 `inlet mix -> ring segment` 上。若必须换算到 ring segment 速度基准，K 会被面积比和流量比分母放大很多：

```text
v_hot / v_ring_seg = (Q_hot/A_hot) / ((0.5 Q_hot)/A_ring)
                   = 2 * A_ring / A_hot
                   ≈ 14.7
```

因此 `K_hot=1` 换算到 ring segment 速度基准约为：

```text
K_ring = 1 * 14.7^2 ≈ 217
```

这个大 K 不是数学错误，但容易造成重复计入。

### 4.6 出口混合节点：`ring segment -> outlet mix -> manifold`

该位置同样是复合构型：

1. 左右 ring segment 流入 outlet mix；
2. 两股流在混合腔中汇合；
3. 混合后进入面积很小的 outlet manifold；
4. 可能伴随突缩、转弯、非均匀入口和二次流。

建议拆分到两个程序连接：

| 连接 | 建议承载的物理损失 | 速度基准 | 推荐 K |
|---|---|---|---:|
| `ring segment -> outlet mix` | 大截面流入混合腔、汇流前扰动 | ring segment 速度 | 0.5 - 1.0，初值 0.7 |
| `outlet mix -> manifold` | 突缩、汇流后加速、进入小支管、转弯 | manifold 速度 | 0.8 - 1.5，初值 1.0 |

由于 manifold 动压约为几百 Pa，`outlet mix -> manifold` 的 K 对总压降非常敏感。

## 5. 映射到当前程序连接

当前 6 段集流环算例中的相关连接类型如下：

| 物理位置 | 程序对象 | 程序连接面积 | 建议设置 |
|---|---|---:|---:|
| `inlet header -> hot leg` | `inlet_buffer_to_hot_leg` (`MacroFlowJunction`) | `AREA_HOT_LEG` | `k_loss = 1.5` |
| `hot leg -> inlet mix` | `hot_leg_to_inlet_mix` (`FlowJunction`) | `AREA_HOT_LEG` | `k_loss = 1.0` |
| `inlet mix -> ring segment` | `segment_entry_links` (`FlowJunction`) | `AREA_RING` | `k_loss = 0.5` |
| `ring segment -> outlet mix` | `segment_exit_links` (`FlowJunction`) | `AREA_RING` | `k_loss = 0.7` |
| `outlet mix -> manifold` | `outlet_mix_to_manifold` (`FlowJunction`) | `AREA_MANIFOLD` | `k_loss = 1.0` |
| `manifold -> outlet header` | `manifold_to_outlet_buffer` (`MacroFlowJunction`) | `AREA_MANIFOLD` | `k_loss = 1.1` |

注意：

- `segment_exit_links` 当前还承接 `RingHP.outlet_dynamic_loss_params`，用于末节点热管管束压降。新增 `k_loss=0.7` 表示出口混合节点相关局部损失，不应替代热管管束动态阻力。
- `RingHP` 内部控制体之间的热管管束阻力已经由 `dynamic_loss_params["model"] == "inline_tube_bank_euler"` 处理，不应再用上述三通 K 重复表示。
- `MacroFlowJunction(multiplier=2)` 表示对称双环折算，`k_loss` 应按单个显式支路的连接面积和支路流量速度基准给出。

## 6. 推荐首轮输入参数

作为当前一维模型的首轮局部阻力输入，可采用：

```text
K_inlet_header_to_hot_leg   = 1.5
K_hot_leg_to_inlet_mix      = 1.0
K_inlet_mix_to_ring_segment = 0.5
K_ring_segment_to_outlet_mix = 0.7
K_outlet_mix_to_manifold    = 1.0
K_manifold_to_outlet_header = 1.1
```

按压降敏感性排序：

1. `outlet mix -> manifold`
2. `manifold -> outlet header`
3. `inlet header -> hot leg`
4. `hot leg -> inlet mix`
5. `ring segment -> outlet mix`
6. `inlet mix -> ring segment`

出口侧更重要的原因是 `A_manifold` 小、速度高，`K=1` 就对应约数百 Pa 的局部压降量级。

## 7. 后续调研重点

后续查阅 Idelchik、Miller、Crane TP-410 或 CFD/实验资料时，应重点确认：

1. tee dividing / combining 的 K 是否基于主管速度、支管速度或合流后速度；
2. 面积比和流量比是否与当前几何相近；
3. 90 度支管、矩形大截面腔体、短混合腔是否需要额外修正；
4. 是否允许局部 K 出现负值。初期工程模型建议把负 K 截断为 0，避免在一维网络中引入难解释的能量补偿；
5. `outlet mix -> manifold` 和 `manifold -> outlet header` 应优先做 CFD 或实验校核。

## 8. 结论

当前局部阻力建模建议采用“分项 K + 明确速度基准”的方式，而不是把所有三通/混合/突扩/突缩合并成一个总 K。

首轮模型中，应优先补充出口侧局部阻力，因为 outlet manifold 的速度最高、动压最大。入口侧局部阻力主要影响分流均匀性和总压降的次要部分。ring segment 连接上的 K 因速度很低，对总压降通常影响较小，但对局部流量分配仍有意义。
