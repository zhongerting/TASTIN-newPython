# V15 V71 Cold Start 1000 s Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run V15 V71 from a uniform 723 K state for 1000 s with TEC disabled, then perform one 27.2 V lookup-based TEC solve and report the final state.

**Architecture:** Add one case-local runner that reuses the V71 builder and existing solver APIs. Keep the thermal run and one-shot TEC solve separable so the TEC process can be stopped without losing the 1000 s restart.

**Tech Stack:** Python 3.12, NumPy, existing TASTIN SystemManager and ThermoCalc lookup runtime.

## Global Constraints

- Use `E:\Users\HC Zhao\anaconda3\envs\tastin-python\python.exe`.
- Initial solid and fluid temperature is 723 K; radiation background is 4 K; fixed core power is 106000 W.
- Disable external heat flux and TEC during the 1000 s thermal-hydraulic run.
- Use `implicit_euler` for solid conduction.
- At t=1000 s, perform exactly one main-series fixed-U TEC lookup solve at 27.2 V.

---

### Task 1: Case-local cold-start runner

**Files:**
- Create: `testModule/Full_Loop_Cases/V15_run_cases_V71/run_v15_v71_cold_start.py`
- Create: `testModule/Full_Loop_Cases/V15_run_cases_V71/test_v15_v71_cold_start.py`
- Modify: `testModule/Full_Loop_Cases/V15_run_cases_V71/README.md`

**Interfaces:**
- Consumes: `build_v15_v71_case_a_system(...)` and existing SystemManager/ReactorCore APIs.
- Produces: a CLI runner, history CSV, 1000 s restart, thermal summary, and one-shot TEC JSON.

- [ ] Write a failing test asserting 723 K initialization, 4 K radiation background, TEC disabled, implicit Euler solids, and normalized V71 profile.
- [ ] Run the focused unittest and confirm failure because the runner API is missing.
- [ ] Implement the minimum runner configuration/build/recording and one-shot TEC functions.
- [ ] Run the focused unittest and existing V71 tests.

### Task 2: Execute and analyze

**Files:**
- Create under: `testModule/Full_Loop_Cases/V15_run_cases_V71/runs/cold_start_723k_1000s/`

- [ ] Run a 1 s smoke with TEC disabled and verify finite temperatures, flows, and saved restart.
- [ ] Run 1000 s with progress records and periodic checkpoints.
- [ ] Invoke the one-shot 27.2 V lookup solve in a separate process; terminate and report if progress stalls.
- [ ] Validate output JSON/CSV/NPZ and analyze final thermal-hydraulic and TEC state.
