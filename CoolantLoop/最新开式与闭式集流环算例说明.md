# 最新开式与闭式集流环算例说明

> 面向后续 Agent 的接管说明。
> 当前日期：2026-06-12。
> 本文只总结当前最新的 6 段钾热管集流环开式/闭式算例、建模方法、几何数据和最近稳定结果。源码与本文不一致时，以源码为准。

## 1. Python 环境

运行本仓库 Python 脚本时使用：

```powershell
& "E:\Users\HC Zhao\anaconda3\envs\tastin-python\python.exe" path\to\script.py
```

## 2. 当前推荐算例

### 2.1 开式回路算例

入口文件：

```text
CoolantLoop/model_collector_ring_6segment_geometry100hp_potassium_mixed.py
```

最新准稳态结果文件：

```text
CoolantLoop/collector_ring_6segment_geometry100hp_potassium_mixed_local_loss_630s_from510s_history.csv
CoolantLoop/collector_ring_6segment_geometry100hp_potassium_mixed_local_loss_630s_from510s_restart.npz
```

630 s 结果：

| 项 | 数值 |
| --- | ---: |
| `T_inlet_buffer_out` | `842.999005 K` |
| `T_out_avg` | `756.536581 K` |
| 入口-出口温差 | `86.462424 K` |
| `W_in_total` | `1.300000 kg/s` |
| `W_out_total` | `1.299999606 kg/s` |
| `W_ring_in_total` | `0.650000 kg/s` |
| `W_ring_out_total` | `0.649999809 kg/s` |

### 2.2 闭式回路算例

入口文件：

```text
CoolantLoop/model_collector_ring_6segment_geometry100hp_potassium_closed_loop.py
```

最新接近稳态结果文件：

```text
CoolantLoop/collector_ring_closed_loop_thermalinit_heater98kw_1200s_from600s_history.csv
CoolantLoop/collector_ring_closed_loop_thermalinit_heater98kw_1200s_from600s_restart.npz
```

1200 s 结果：

| 项 | 数值 |
| --- | ---: |
| `T_inlet_buffer_out` | `843.127417 K` |
| `T_out_avg` | `756.555198 K` |
| 入口-出口温差 | `86.572219 K` |
| `W_total_loop` | `1.298584 kg/s` |
| `W_ring_in_total` | `0.649292 kg/s` |
| `W_ring_out_total` | `0.649292 kg/s` |
| `Q_heater` | `98.000 kW` |
| `Q_reject_est` | `98.000114 kW` |
| `Q_balance_residual` | `-0.002551 kW` |

温度漂移判据：

| 时间段 | 入口温度漂移 | 出口温度漂移 |
| --- | ---: | ---: |
| `1000 -> 1200 s` 折算 | `-0.00989 K/100s` | `-0.00977 K/100s` |
| `1100 -> 1200 s` | `-0.00742 K/100s` | `-0.00733 K/100s` |

因此闭式回路按最近 `100-200 s` 判据已满足温度漂移 `< 0.01 K/100s`。

### 2.3 V9 接口温度独立预调试算例

入口文件：

```text
CoolantLoop/model_collector_ring_6segment_v9_interface.py
```

该算例用于 V10 融合前的接口预调试，不是完整 V10。它保留当前 6 段集流环、100 根钾热管、翅片和辐射设置，但把三条热侧入口支路和三条冷侧出口支路改为 V9 接口口径。

热侧入口温度取 V9 带 TEC 3000 s 结果中 `HotOutletBranch_1/2/3` 出口温度平均值：

```text
T_inlet = 838.944495 K
```

V9 接口几何：

| 部件 | 数量 | 长度 m | 节点数 | 流通面积 m2 | 水力直径 m |
| --- | ---: | ---: | ---: | ---: | ---: |
| `HotOutletBranch_1/2/3` | 3 | `2.19632` | 8 | `5.9798e-4` | `0.0276` |
| `Manifold_1` / #38 | 1 | `0.40911` | 5 | `2.5434e-4` | `0.0180` |
| `Manifold_2` / #44 | 1 | `1.41912` | 17 | `2.5434e-4` | `0.0180` |
| `Manifold_3` / #50 | 1 | `1.41912` | 17 | `2.5434e-4` | `0.0180` |

运行命令：

```powershell
& "E:\Users\HC Zhao\anaconda3\envs\tastin-python\python.exe" CoolantLoop\model_collector_ring_6segment_v9_interface.py `
  --case-name collector_ring_6segment_v9_interface_200s `
  --t-end 200 `
  --max-dt 0.5 `
  --print-every-time 20 `
  --restart-save-every 20
```

200 s 结果文件：

```text
CoolantLoop/collector_ring_6segment_v9_interface_200s_history.csv
CoolantLoop/collector_ring_6segment_v9_interface_200s_restart.npz
```

200 s 结果：

| 项 | 数值 |
| --- | ---: |
| `T_inlet_buffer_out` | `838.943506 K` |
| `T_out_avg` | `753.788971 K` |
| `T_outlet_buffer_out` | `754.381386 K` |
| 入口-出口平均温差 | `85.154535 K` |
| `W_in_total` | `1.300000 kg/s` |
| `W_out_total` | `1.299935 kg/s` |
| `W_ring_in_total` | `0.650000 kg/s` |
| `W_ring_out_total` | `0.649971 kg/s` |
| 估算散热量 `m_dot*(h_in-h_out_avg)` | `96.501911 kW` |
| `T_out_avg` 漂移 `180 -> 200 s` | `-1.857603 K/100s` |

200 s 时尚未严格稳态，但流量倍率和接口温度口径正确。若需要更接近稳态的 V10 初值，可从 `collector_ring_6segment_v9_interface_200s_restart.npz` 继续续算。

## 3. 开式回路建模

开式回路拓扑：

```text
inlet_boundary
  -> inlet_buffer
  -> MacroFlowJunction x3
  -> hot_leg_single x3
  -> inlet_mix_node x3
  -> A1..A6 segmented collector ring
  -> outlet_mix_node x3
  -> manifold_single x3
  -> MacroFlowJunction x3
  -> outlet_buffer
  -> outlet_boundary
```

建模要点：

- 使用一个显式集流环代表两个对称集流环。
- 总物理流量 `W_TOTAL=1.3 kg/s`。
- 显式单环流量约 `0.65 kg/s`。
- `MacroFlowJunction(multiplier=2)` 只用于宏观总管和单环支路之间的质量/能量倍率。
- 开式入口边界给定流量和入口温度，出口边界给定压力。
- 入口/出口混合节点不带热管，只负责支管和集流环的连接混合。

## 4. 闭式回路建模

闭式回路拓扑：

```text
Pressurizer
  -> PumpJunction
  -> HeaterChannel
  -> InletBuffer
  -> HotLeg_1/2/3
  -> InletMix_I1/I2/I3
  -> A1..A6 segmented collector ring
  -> OutletMix_O1/O2/O3
  -> Manifold_1/2/3
  -> OutletBuffer
  -> ReturnChannel
  -> Pressurizer
```

闭式回路相对开式回路的变化：

- 删除 `inlet_boundary`、`outlet_boundary` 和强制流量 `InletJunction`。
- 新增 `PressurizerVolume` 作为唯一压力参考点。
- 新增 `PumpJunction` 作为主泵，默认压升 `2070 Pa`。
- 新增 `HeaterChannel`，默认加热功率 `98 kW`。
- 新增 `ReturnChannel`，把出口缓冲体接回稳压器。
- 保留开式回路的 6 段 RingHP、局部阻力、热管求解器和辐射设置。

闭式回路默认初始化：

- 流体初始温度采用开式准稳态型分布：
  - 加热器入口、回流侧、出口侧：`756.5366 K`
  - 加热器出口、集流环入口侧：`843 K`
- 固体热状态从开式准稳态 restart 迁移：

```text
CoolantLoop/collector_ring_6segment_geometry100hp_potassium_mixed_local_loss_630s_from510s_restart.npz
```

迁移内容：

- 6 个集流环壁面固体。
- 18 个代表热管内部固体。
- 不迁移开式水力网络，因为闭式拓扑不同。

## 5. 共同运行参数

| 参数 | 数值 |
| --- | ---: |
| 冷却剂 | NaK |
| 热管工质 | Potassium |
| 外部辐射环境温度 `T_SPACE` | `200 K` |
| 开式入口目标温度 / 闭式加热器出口目标附近 | `843 K` |
| 压力参考 / 开式出口压力 | `160000 Pa` |
| 总物理流量目标 | `1.3 kg/s` |
| 显式单环流量目标 | `0.65 kg/s` |
| 热管总数 | `100` |
| 热管发射率 | `0.6` |
| 翅片发射率 | `0.6` |
| 集流环壁面发射率 | `0.05` |

## 6. 管道和控制体几何

### 6.1 主回路管道

| 部件 | 数量 | 长度 m | 节点数 | 单节点长度 m | 流通面积 m2 | 水力直径 m |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `HeaterChannel` | 1 | `1.00000` | 10 | `0.10000` | `1.7948547e-3` | `0.0478046` |
| `InletBuffer` | 1 | `0.20000` | 5 | `0.04000` | `1.7948547e-3` | `0.0478046` |
| `HotLeg` | 3 | `2.19632` | 28 | `0.07844` | `5.9828490e-4` | `0.0276000` |
| `A1..A6` 集流环段 | 6 | `0.79300` | 3 | `0.26433` | `4.4000000e-3` | `0.0586667` |
| `Manifold` | 3 | `0.40911` | 5 | `0.081822` | `2.5446900e-4` | `0.0180000` |
| `OutletBuffer` | 1 | `0.20000` | 5 | `0.04000` | `7.6340701e-4` | `0.0311769` |
| `ReturnChannel` | 1 | `1.00000` | 10 | `0.10000` | `7.6340701e-4` | `0.0311769` |
| `Pressurizer` | 1 | `0.20000` | 1 | `0.20000` | `7.6340701e-4` | `0.0311769` |

`Pressurizer` 体积：

```text
V_pressurizer = 1.5268140e-4 m3
```

### 6.2 集流环矩形通道

| 项 | 数值 |
| --- | ---: |
| 矩形截面长度 | `0.110 m` |
| 矩形截面宽度 | `0.040 m` |
| 流通面积 `AREA_RING` | `0.0044 m2` |
| 湿周 `PERIM_HEADER` | `0.300 m` |
| 水力直径 `DH_RING` | `0.0586667 m` |
| 等效内半径 `R_IN_RING` | `0.0477465 m` |
| 壁厚 | `0.002 m` |
| 等效外半径 `R_OUT_RING` | `0.0497465 m` |

### 6.3 混合节点

| 节点 | 数量 | 长度 m | 体积 m3 | 面积基准 | 水力直径 m |
| --- | ---: | ---: | ---: | --- | ---: |
| `InletMix_I1/I2/I3` | 3 | `0.0552` | `3.3025327e-5` | `AREA_HOT_LEG` | `0.0276` |
| `OutletMix_O1/O2/O3` | 3 | `0.0360` | `9.1608842e-6` | `AREA_MANIFOLD` | `0.0180` |

混合节点不带热管，不使用集流环控制体尺寸；但混合节点与集流环段连接时，连接面积使用 `AREA_RING`。

## 7. 6 段集流环和热管数量分配

集流环由 6 个非闭合 `1/6 RingHP` 段组成，每段 3 个流体控制体。

连接顺序：

```text
I1 -> A1 -> O1 -> A2 -> I2 -> A3 -> O2
   -> A4 -> I3 -> A5 -> O3 -> A6 -> I1
```

热管数量分配：

| 段 | 连接 | 节点热管数 | 小计 |
| --- | --- | ---: | ---: |
| A1 | `I1 -> O1` | `[5, 6, 6]` | 17 |
| A2 | `O1 -> I2` | `[5, 5, 6]` | 16 |
| A3 | `I2 -> O2` | `[5, 6, 6]` | 17 |
| A4 | `O2 -> I3` | `[5, 5, 6]` | 16 |
| A5 | `I3 -> O3` | `[5, 6, 6]` | 17 |
| A6 | `O3 -> I1` | `[5, 6, 6]` | 17 |

总数：

```text
17 + 16 + 17 + 16 + 17 + 17 = 100
```

## 8. 热管与翅片几何

| 参数 | 数值 |
| --- | ---: |
| 热管总长度 | `0.600 m` |
| 蒸发段长度 | `0.100 m` |
| 绝热段长度 | `0.000 m` |
| 冷凝段长度 | `0.500 m` |
| 蒸汽区半径 | `0.0075 m` |
| 吸液芯厚度 | `0.0005 m` |
| 管壁厚度 | `0.0010 m` |
| 热管内半径 | `0.0080 m` |
| 热管外半径 | `0.0090 m` |
| 热管外径 | `0.0180 m` |
| 吸液芯孔隙率 | `0.6` |
| 翅片平均宽度/高度 | `0.020 m` |
| 翅片厚度 | `0.0004 m` |
| 翅片覆盖范围 | 完全覆盖冷凝段 |

热管离散：

| 项 | 数值 |
| --- | ---: |
| 蒸发段轴向节点 `HP_N_EVA` | 1 |
| 绝热段轴向节点 `HP_N_ABA` | 0 |
| 冷凝段轴向节点 `HP_N_CON` | 12 |
| 吸液芯径向节点 `HP_N_WICK` | 1 |
| 管壁径向节点 `HP_N_WALL` | 2 |
| 翅片高度方向节点 `N_FIN_HEIGHT` | 15 |

## 9. 热管和导热求解设置

当前热管设置：

| 项 | 设置 |
| --- | --- |
| 吸液芯导热 | 各向异性 |
| 轴向导热上限 `k_cap` | `None` |
| 热管时间推进 | `theta_implicit` |
| `theta` | `0.6` |
| 热管辐射边界隐式化 | 开启 |
| 集流环壁面固体 ODE 方法 | `LSODA` |

注意：

- 热管内部导热不走通用 BDF 路径，而使用热管专用的 `theta_implicit`。
- 热管和翅片辐射边界已进行热管专用隐式线性化。
- 集流环壁面仍是通用 `HeatConduction2D`，当前默认 ODE 方法是 `LSODA`。

## 10. 局部阻力设置

| 连接 | 固定 `K` | 说明 |
| --- | ---: | --- |
| `Pump -> Heater` | `0.0` | 闭式新增 |
| `Heater -> InletBuffer` | `0.2` | 闭式新增 |
| `InletBuffer -> HotLeg` | `1.5` | 宏观总管到单环支路 |
| `HotLeg -> InletMix` | `1.0` | 进口支管到进口混合体 |
| `InletMix -> RingSegment` | `0.5` | 进口混合体到集流环段 |
| `RingSegment -> OutletMix` | `0.7` | 另叠加 `RingHP.outlet_dynamic_loss_params` 热管管束动态阻力 |
| `OutletMix -> Manifold` | `1.0` | 出口混合体到出口支管 |
| `Manifold -> OutletBuffer` | `1.1` | 单环出口支管到宏观总管 |
| `OutletBuffer -> Return` | `0.2` | 闭式新增 |
| `Return -> Pressurizer` | `0.2` | 闭式新增 |

热管管束压降：

- 集流环内部节点间继续使用 `inline_tube_bank_euler` 动态阻力。
- `RingSegment -> OutletMix` 保留末节点热管管束动态阻力，并额外叠加出口混合局部阻力 `K=0.7`。
- 不要把固定 `K=0.7` 当成热管管束压降的替代项。

## 11. 闭式回路泵和加热器

闭式回路默认泵压升：

```text
DEFAULT_PUMP_DELTA_P = 2070 Pa
```

来源：

- 开式局部阻力后路径压降约 `1.622 kPa`。
- 闭式新增回流管、加热管和连接损失。
- `Q_HEATER=0` 泵压升扫描：
  - `2000 Pa -> W_total_loop ≈ 1.276 kg/s`
  - `2200 Pa -> W_total_loop ≈ 1.344 kg/s`
- 插值得到 `2070 Pa` 附近可达到约 `1.3 kg/s`。

闭式回路默认加热功率：

```text
DEFAULT_HEATER_POWER = 98 kW
```

来源：

```text
Q ≈ 1.3 kg/s * (h(843 K) - h(756.54 K)) ≈ 97.98 kW
```

加热器实现方式：

- 在 `HeaterChannel` 流体控制体上施加持久热源。
- 通过 `SystemManager.add_persistent_fluid_source()` 注册。
- 每个加热器流体节点平均分配 `Q_HEATER / N_HEATER`。

## 12. V9 堆芯侧开式外管路骨架

`testModule/test_core_assemble_v9_caseA.py` 新增了一个不包含集流环和热管的堆芯侧开式外管路骨架，用于后续与本文件中的 6 段集流环三进三出接口融合前的调试。V9 不修改 V8 默认算例；它复用 V8 堆芯、TEC、NaK78 工质和导线电阻，然后重建水力网络。

V9 拓扑方向为：

```text
fixed-flow inlet
  -> V9_InletConnector
  -> #38 + #44/#50 representative branch
  -> #53 radiator inner header
  -> #52 radiator outer header
  -> cold return branches
  -> CoreInletConnector
  -> V8/V9 core representative channels
  -> CoreOutletConnector
  -> three hot outlet branches
  -> fixed-pressure outlet
```

与本文件中的集流环开式算例相比，接口语义相反：

- 集流环开式算例的入口是堆芯出口热流体进入集流环的一侧，默认温度口径约 `843 K`。
- V9 的入口是未来集流环出口侧、也就是返回堆芯前的冷侧，默认先使用 `743 K`。
- V9 的出口是未来集流环入口侧，后续应连接到集流环 `I1/I2/I3` 三个入口混合节点。

V9 第一版约束：

- 不含 `RingHP`、热管、翅片或辐射散热。
- 不加泵、不加稳压器；入口固定总质量流量，出口固定压力。
- 不使用集流环局部阻力系数，所有新增外管路局部 `K=0.0`。
- #38 单独建模，#44/#50 用 `multiplier=2` 代表；冷回流 #2/#3 用 `multiplier=2` 代表；三条堆芯出口热支路显式保留。
- 冷态水力/拓扑 smoke 使用 `run_v9_caseA_open_loop.py --disable-tec-coupled`；带 TEC 的 V9 计算应从 V9 兼容的温热 restart 或后续专用迁移器开始。

未来与 6 段集流环连接时，必须保留“两套对称集流环、只显式建一套”的倍率口径：

- V9 的 `HotOutletBranch_1/2/3` 是宏观总系统支路，每条约 `0.433 kg/s`。
- 当前集流环算例显式建模单个集流环，因此进入单环 `I1/I2/I3` 的流量应约为 `0.2167 kg/s`。
- `HotOutletBranch_1/2/3 -> InletMix_I1/I2/I3` 之间需要宏观到单环的 `MacroFlowJunction(multiplier=2)` 或等价连接。
- `OutletMix_O1/O2/O3 -> V9 冷侧外管路` 也需要单环到宏观管路的匹配 `MacroFlowJunction(multiplier=2)`。
- 不得把三条热出口支路直接硬接到单个显式集流环的 `I1/I2/I3`，否则质量流量和能量会放大错误。

## 13. 重要注意事项

1. 闭式回路不能直接加载开式全局 restart，因为水力拓扑不同。
2. 闭式回路默认只从开式 restart 迁移固体热状态，不迁移开式流体网络。
3. 若修改 `MacroFlowJunction(multiplier=2)`，必须重新核对总流量、单环流量和能量统计口径。
4. 若修改热管数量分配，必须同时核对换热面积、热管管束压降和热源/散热统计。
5. 若继续调闭式稳态温度，优先微调 `Q_HEATER` 或泵压升；不要先改热管辐射/换热模型。
6. 若新增真实回流管、加热管或三通局部阻力，应重新做泵压升扫描。
7. `Q_reject_est` 当前主要按流体焓差估算；详细能量闭合仍应结合储能项和热管/辐射诊断。

## 14. 建议后续 Agent 的最小阅读路径

如果继续处理集流环闭式/开式回路，建议按以下顺序读：

1. 本文。
2. `CoolantLoop/AI_AGENT_COOLANTLOOP_ANALYSIS.md`。
3. `CoolantLoop/model_collector_ring_6segment_geometry100hp_potassium_mixed.py`。
4. `CoolantLoop/model_collector_ring_6segment_geometry100hp_potassium_closed_loop.py`。
5. 若涉及水力求解，读 `Solvers/HYDRODYNAMICS_DETAILED_INTRO.md` 和 `Solvers/Hydrodynamics/Components.py`。
6. 若涉及热管或辐射，读 `Components/BASICCOMPONENTS_DETAILED_INTRO.md`、`Components/RingHP.py`、`Components/HPwithFin.py` 和 `Components/basicComponents/HeatPipe2D.py`。

## 15. 2026-06-12 V10 堆芯-集流环开式融合算例

V10 融合算例位于：

```text
testModule/test_core_assemble_v10_caseA.py
testModule/run_v10_caseA_open_loop.py
```

它不是新的独立集流环生产模型，而是在 V9 堆芯开式骨架上嵌入本文件的 6 段钾热管集流环接口模型。V10 继续使用一个显式集流环代表两个对称集流环的口径，`HotOutletBranch_1/2/3` 到 `InletMix_I1/I2/I3` 使用 `MacroFlowJunction(multiplier=2)` 从宏观支路分到单环；`Manifold_1/2/3` 到冷侧总管也使用对应的 `multiplier=2` 汇流。

V10 拓扑为：

```text
fixed-flow inlet
  -> CoreInletConnector
  -> core representative channels
  -> CoreOutletConnector
  -> HotOutletBranch_1/2/3
  -> InletMix_I1/I2/I3
  -> A1..A6 RingHP collector ring
  -> OutletMix_O1/O2/O3
  -> Manifold_1/2/3
  -> V10_RadiatorManifoldMerge
  -> RadiatorInnerHeader_53
  -> RadiatorOuterHeader_52
  -> ColdReturnBranch_1 + ColdReturnBranch_2_3_Rep
  -> fixed-pressure outlet
```

与 V9 相比，V10 删除了堆芯侧与集流环重复的上游冷侧接口支路；`InletMix_*`、`A1..A6`、`OutletMix_*` 和 `Manifold_*` 由集流环模型提供。V10 仍然不加泵、不加稳压器，沿用固定入口总流量和出口定压边界。

V10 初始化不从零冷态启动。默认注入：

- V9 堆芯侧 restart：`testModule/v9_caseA_open_loop_tec_3000s/v9_caseA_open_loop_tec_3000s_latest_restart.npz`
- 集流环接口 restart：`CoolantLoop/collector_ring_6segment_v9_interface_500s_from200s_restart.npz`

注入后必须保留集流环内部 `A*` 环段、`J_I*/J_A*` 段间连接、`OutletMix -> Manifold` 和 `Manifold_*` 内部通道流量分布；不能再把这些 junction 统一重置为设计正向流量。外部边界、堆芯通道、热出口支路、冷回流支路仍按 V10 设计流量重置。

已完成验证：

```powershell
& "E:\Users\HC Zhao\anaconda3\envs\tastin-python\python.exe" -m py_compile testModule\test_core_assemble_v10_caseA.py testModule\run_v10_caseA_open_loop.py testModule\test_v10_caseA_topology.py
& "E:\Users\HC Zhao\anaconda3\envs\tastin-python\python.exe" -m unittest testModule.test_v10_caseA_topology
```

两项均通过。V10 初始化 restart 已成功写入 `testModule/v10_caseA_open_loop_init/`，从该 restart 带 TEC 续算 `40 s` 可完成；末端约为 `Tin=743.000 K`、`Tcore_out=838.918 K`、`Tring_out=762.823 K`、`Pel=4874.558 W`。当前 V10 带 TEC 计算成本较高，长时稳态计算建议先采用 `max_dt=0.05 s` 的保守分段续算，后续再优化启动步长和热管/水力求解性能。
